{% doc %}
  Renders the size guide block. Uses product metafield `custom.size_guide`.

  @param {object} product_object - Product to resolve size guide content from

  @example
  {% render 'size-guide', product_object: product %}
{% enddoc %}


{{ product_object | json }}
